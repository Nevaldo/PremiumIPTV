
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.5.1"
    }
}

allprojects {
    // Repositórios agora são gerenciados no settings.gradle
}

task clean(type: Delete) {
    delete rootProject.buildDir
}